<!--pages/wxs/wxs.wxml-->
<!-- <text>pages/wxs/wxs.wxml</text> -->
<!--1. wxs代码块 -->
<!-- 不仅要有module 你还需要将写好的wxs导出 -->
<!-- <wxs module="info" >
  var name = 'LBJ';
  var age = 35;
  var sum = function(num1,num2){
  return num1 + num2
  }
  //不仅要有module 你还需要将写好的wxs导出 
  module.exports={
  name:name,
  age:age,
  sum:sum
  };
</wxs> -->
<!-- wxs使用 -->
<!-- 定义在wxs文件中，在使用wxs标签引用 只能使用相对路径-->
<wxs src="../../wxs/info.wxs" module="info" />
<view>{{info.name}}</view>
<view>{{info.age}}</view>
<view>{{info.sum(20,30)}}</view>
<wxs src="../../wxs/format.wxs"  module="format" />
<view>{{format.toformat(price,2)}}</view>
<!-- 利用单独的wxs文件里面的脚本,将时间戳转化为字符串格式 -->
<view>{{format.dateFormat(time,'yyyy-MM-dd hh:mm:ss')}}</view>